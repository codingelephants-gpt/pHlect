from setuptools import setup, find_packages

setup(
    name='pHlect',
    version='0.1.0',
    packages=find_packages(),
    author='codingelephants-gpt',
    description='pHlect is a Python package for reading files.',
    python_requires='>=3.6',
)